# dbt_project.yml
name: dbt_project       # doit correspondre au nom dans profiles.yml
version: 1.0.0
config-version: 2

profile: dbt_project    # <- lien avec profiles.yml

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"        # dossier compilation
clean-targets: ["target", "dbt_packages"]

models:
  good_air_project:
    +materialized: table
